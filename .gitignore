/stormlight
/dist/
/.DS_Store
/.local/
*.log
/.claude/

# The frontend toolchain. web/dist is committed on purpose: it is
# embedded in the binary, so `go build ./...` must work without Node.
web/node_modules/
